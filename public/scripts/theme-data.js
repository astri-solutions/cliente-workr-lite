// scripts/theme-data.js
// Default placeholder for local dev / a not-yet-published template checkout.
// publish-config and provision-portal overwrite this per portal with the
// real colors/fonts (and it's excluded from the scripts/ self-heal sync so
// that overwrite is never reset back to this empty default).
window.__WL_THEME__ = {};
